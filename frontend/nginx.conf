# SPA (React Router): qualquer rota sem arquivo correspondente cai no index.html.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
